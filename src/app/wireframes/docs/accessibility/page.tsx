import { WireframeDocStub } from "@/components/wireframes/doc-stub";

export default function Page() {
  return (
    <WireframeDocStub
      blurb="prefers-reduced-motion with real static fallbacks — not a disabled animation."
      route="/wireframes/docs/accessibility"
      title="Accessibility"
    />
  );
}
