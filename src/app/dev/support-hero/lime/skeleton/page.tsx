"use client";

import { SupportHeroSkeleton } from "@/screens/support-hero/support-hero-skeleton";
import { SUPPORT_HERO_THEME_LIME } from "@/screens/support-hero/themes";

export default function DevSupportHeroLimeSkeletonPage() {
  return <SupportHeroSkeleton theme={SUPPORT_HERO_THEME_LIME} />;
}
