import { redirect } from "next/navigation";

/** Dev preview entry — skeleton only (live is not in catalog). */
export default function DevPicdropDashboardIndexPage() {
  redirect("/dev/picdrop-dashboard/skeleton");
}
