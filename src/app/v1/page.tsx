import { FramelineHomePageV1 } from "@/components/frameline-home-page-v1";

/** Frozen snapshot of the Phase-02 marketing landing. Production root is / (v2). */
export default function HomeV1() {
  return <FramelineHomePageV1 />;
}
